[theme]
primaryColor="#4F8BF9" # This is a bright blue, we'll override later with custom CSS
backgroundColor="#FFFFFF" # White background
secondaryBackgroundColor="#F0F2F6" # Light gray for cards/secondary elements
textColor="#303030" # Dark gray for text
font="sans serif"

# Use this to globally override Streamlit's base theme if needed.
# However, we will use targeted CSS for the dropdown fix.
